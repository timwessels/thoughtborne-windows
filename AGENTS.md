# AGENTS.md

Guidance for AI coding agents working in this repository. Plain Markdown — humans are welcome to read it too.

## What this is

Thoughtborne is a hotkey-driven voice-to-text tool for Windows, written in Python. Recording is started with `Ctrl+Alt+W`; one of `A` / `D` / `H` / `Y` (held with `Ctrl+Alt`) ends it and inserts the transcript at the cursor position in whatever Windows app is active. Four transcription APIs are switchable at runtime; the default is `soniox-live`. Windows-only by design (Win32 hotkeys, audio capture, text insertion).

- **Setting the tool up for a user?** Follow [`llms-install.md`](llms-install.md) — the guided setup, including API-key onboarding and the WSL2 pitfalls.
- **Vision & direction:** [`VISION.md`](VISION.md) — why the tool exists, the quality bar (*good enough to send to LLMs unread*), who it's for, decision principles, and non-goals. Calibrate judgment calls — scope, trade-offs, priorities — against it. Stability is principle #1; the quality floor and the no-training-on-user-data rule are hard gates. The target user, in one sentence: a self-reliant, German-dictating techie who wants no subscription, no lock-in, verifiable privacy, and a tool that bends to their workflow (full persona under "Who it's for" in `VISION.md`).
- **Mac port:** [`thoughtborne-macos`](https://github.com/timwessels/thoughtborne-macos) — three APIs instead of four, otherwise analogous. Useful as a reference for porting decisions.

## Run & verify

- Run: `uv run thoughtborne.py`, or double-click `Thoughtborne.bat`. Requires Windows, a microphone, and at least one API key in `.env` (template: `.env.example`).
- Working from inside WSL2? The tool itself must run Windows-side; [`llms-install.md`](llms-install.md) covers the interop.

Verification is a ladder of standalone off-Windows test drivers — every `test_*.py` in the repo root is a plain script that runs and reads on its own, not a pytest suite, though since #242 pytest can collect it as well (*Test ladder* below). `python3 run_tests.py` runs the whole ladder in one command: one verdict line per driver, non-zero exit if any driver failed, and each driver's `(skipped ...)` notes surfaced, so a green run never reads as if a skipped check had passed. CI runs that same command on every push and pull request ([`.github/workflows/tests.yml`](.github/workflows/tests.yml), Python 3.10 + 3.12 under `xvfb-run`, so the display-gated checks run for real). Below the ladder sits `python -m py_compile <changed files>` for syntax; above it sit the rungs no automation reaches — start the tool and check the startup masthead plus `All hotkeys registered successfully` in `thoughtborne.log`, and let the in-app self-test `Ctrl+Alt+T` transcribe `test_audio.mp3` end to end (needs a valid API key).

One row per driver, in the order `run_tests.py` runs them. The detail behind a row lives in that driver's module docstring, which is where it is maintained — read it before changing what a driver covers.

| Driver | What it covers | Flags |
| --- | --- | --- |
| `test_archive_migration.py` | `config.migrate_legacy_archives` against tempdir layouts (#50, #242) — both/one/no legacy folders, the BOTH-exist refusal, a half-finished run, a cross-volume `EXDEV` failure that heals on the next run, hostile layouts; every case is asserted against the files on disk (events by substring, they are user-facing prose) and any escaping exception is itself a failure, since the risk here is years of recordings landing somewhere unexpected in silence. | `--show` (each case's events) |
| `test_audio_stall.py` | The #128 audio stall/deadlock guards against a fault-injecting fake stream — the non-blocking capture read, the stall watchdog, and the close/open timeout-and-poison containment. | — |
| `test_config_loading.py` | The #206 hardened `personal_settings.json` reader and the #238 `.env` load against tempdir fixtures, each fixture re-checked by a real `import config` in a subprocess. Named only here: the two static guards behind the reader — `main()` calls `replay_import_warnings()`, and `config.py` makes no module-level `_config_logger` call (the stderr-only lane #206 closed and #238 inherits). The real Windows start with a broken file stays hands-on. | `--show`; the Soniox lane needs `groq`, the `.env` lanes `python-dotenv` — both installed in CI, skipped cleanly without |
| `test_console_ui.py` | Every console panel/strip: widths, CP437 charset, the plain-ASCII twin, red-exclusivity, grid anchors, logo fold-in. Named only here: the #166 ALREADY RUNNING notice and the partial-registration advisory, the #200 key-aware lineup where a row whose key env var is absent renders dim (framed and compact renderer, checked positionally), the keyless shop-window masthead's yellow guidance line, the calm keyless SETUP-NEEDED panel a hotkey press raises, the `(default)`-marker-gone guard across masthead/switched/switch-failed, and the pure `config.engine_has_key` predicate plus its `API_KEY_ENV == AVAILABLE_APIS` coverage guard. | `--show` (every screen, shop-window masthead and keyless panel included) |
| `test_deps_sync.py` | The two dependency-declaration lanes in lockstep (#173) — `pyproject.toml`'s `[project] dependencies` against `requirements.txt` as a normalized requirement set, naming the drifting package and which file holds which version; `tomllib` where present, a line parser on 3.10, the two cross-checked when both exist. | `--show` (parsed sets + active parser) |
| `test_engine_memory.py` | The #193/D-008 memory of the last selected engine against a tempdir — the `runtime_state.json` round-trip and its atomic, leftover-free write, the `AVAILABLE_APIS` whitelist on both sides, the corrupt / stale / unwritable-directory fallbacks that must never cost a start, the startup precedence rule, the shipped carousel rotation, and the static guard on both directions of `config.py`'s `DEFAULT_API_IS_EXPLICIT`. | — |
| `test_hotkey_overrides.py` | The #55 `personal_settings.json` hotkey/engine override surface — the `hotkey_parse` lexical layer and `config.apply_hotkey_overrides` (partial override, warn-and-fallback, duplicate/collision handling, F-keys, list values). Named only here: the per-box display-lead prefix (`hotkey_parse.common_prefix`) and the #211/D-012 static-default guard that every key in `DEFAULT_HOTKEYS` classifies `KEY_STATIC`, so no shipped default can regress onto a layout-resolved key. | `--show` (a few effective sets) |
| `test_ptt_detector.py` | The #66 push-to-talk gesture state machine (#242) against synthetic tick sequences, every case compared as the exact per-tick action list: the three windows landing exactly on their boundary, tap expiry, early release, release-tail re-press, both vetoes, `reset()`'s phantom-edge guard, and the `needs_foreign_scan()`/`_STEADY_STATES` pin. Named only here: timestamps are multiples of 1/16 s, so those comparisons are binary-exact instead of float noise. | — |
| `test_restart_signal.py` | The #202 settings-app→tool restart handshake against a tempdir — the signal file's write/consume round-trip and the no-shutdown-without-a-successful-consume invariant. Named only here: the probe never creating or holding the mutex (fail-open to False off-Windows), the constants (`TOOL_MUTEX_NAME == "Thoughtborne-SingleInstance"`, distinct from `settings_instance.SETTINGS_MUTEX_NAME`, wait > interval), the source guards that the old literal is gone and `consume_restart_signal` is wired in both the startup guard and the recording loop, and that the live mutex probe plus the end-to-end restart stay hands-on. | — |
| `test_retry_marker_lifecycle.py` | The persistent retry-marker lifecycle against a tempdir archive (D-001) — #106/#114/#134 announce-once, the #133/#141 no-speech verdict and #138 generalizing it to every engine on a single-engine basis, with the per-call error signal and its coarse reason category. Plus the #241 import-side guard that importing `output_handler` leaves pyautogui's corner fail-safe off (asserted on the stub module). | — |
| `test_settings_instance.py` | The #196/D-009 settings single-instance guard — lazy ctypes, the four localized window titles computed from `settings_strings`, the distinct and session-scoped `SETTINGS_MUTEX_NAME`, the `FOCUS_*` categories, the fail-open behaviour off-Windows, and the #222 one-exit source guard on `thoughtborne.py`; the real ctypes mutex/focus is hands-on (#199, #222). | — |
| `test_settings_io.py` | The #144 settings-app IO core — the surgical `.env` / `personal_settings.json` merges with their data-safety regressions, the pure hotkey-combo helpers, `key_check` including the #205 honesty contract, the DE/EN string table (D-015), the three-valued `defaults.api` (#193/#198), `engine_keyed` (#201), `resolve_fixed_entry_engine` (#207), the push-to-talk toggle (#233) and the gated language persist (#239). Named only here: the pure `resolve_save_action` decision table (first_run × has_key → `save`/`save_close`/`save_restart`, no `tool_running` axis and no `save_start` since #223/D-014 — the settings window opens only from a running tool, so a keyed save is always a restart — the token space asserted exactly, plus the guard that the retired `btn.save_start`/`footer.*` strings stay gone), the #211 preset-parity guard (`PRESET_FKEYS`' four housekeeping actions equal `config.DEFAULT_HOTKEYS`'), `engine_keyed` delegating to `config.engine_has_key` so console and settings never disagree, and the push-to-talk shipped-default guard read off `config.py` as source rather than from the imported value. | `--show` (a sample `.env` + settings write) |
| `test_settings_theme.py` | The #155/D-010 theme module — lazy tkinter import, every palette constant a `#RRGGBB` string, WCAG AA (≥ 4.5:1) for every text colour on both surfaces and the 3:1 non-text floor for `CONTROL_LINE`, the type-ladder floor (`SIZE_BODY` ≥ 10, small→title strictly increasing), and with a display the `clam` pin plus `TFrame` background == `PAGE` (the #180 sync invariant). | display-gated part skips without a display, runs for real under `xvfb-run` and in CI |
| `test_settings_visibility.py` | The #203 tkinter-free helpers (`scrollbar_should_show`, `format_visible_line`, `wrap_length`) and, since #240, the whole `[SETTINGS]` log lane with its three AST guards on the app. With a display, regressions against the real app: the #180/#203 auto-hide idempotency and wrap deferral (the GDI cost is Windows-only, so the perf proof is the maintainer's map→expose measurement), the #216 maximize→restore cycle, the #231 verdict wrap with the #179 balance note, the #239 language-toggle gate, and the two crash lanes. Named only here: the #216 check also asserts the body stays centred (physical x == recorded item x), the `after_idle` crash lane asserts delegation to Tk's stock handler, a still-usable window and a held flood cap, and the #240 AST guards include the `IMPORT_WARNINGS` replay. | `--show`; display-gated parts skip without a display, run for real under `xvfb-run` and in CI; the writing lanes patch `config.SCRIPT_DIR` / `config.LOG_FILE` to a tempdir |
| `test_setup.py` | Static drift guard on the shipped install mechanics (#76) — it reads the scripts as text, no PowerShell runs, so real behaviour belongs to the hands-on Windows-Sandbox check under `sandbox/`. Its docstring names none of the guards, so they are listed here (each is a `test_*` function in the driver): `setup.ps1`'s ASCII/no-BOM, no bare `exit` (iex-safety), the user-data denylist, the fingerprint and running-instance guards, `-DryRun` gating, the Start-menu shortcut, no secret collection (D-002), the Astral-uv launcher fallbacks, and the #244 failure containment — steps 7 and 7b each wrapped in their own try/catch so a shortcut or a registration failure warns and carries on into flag-driven closing lines, with `-ErrorAction Stop` on every `$regPath` cmdlet, without which that catch would never fire; the #209 uninstall story — the per-user Apps-list registration (cmdlet-based, DryRun-gated, `DisplayVersion` parsed from the installed `pyproject.toml`, a real `DisplayIcon` whose asset exists, `QuietUninstallString` carrying `-Silent`) and `uninstall.ps1` (ASCII/no-BOM, the keep-list covering user data while excluding `.venv` and `.env.example`, the console-hide, the never-kill running guard referencing `Ctrl+Alt+4`, the self-copy plus captured-`-InstallDir` threading, the registry-key self-delete, shortcut removal, uv left untouched, and the D-011 contract: checkbox default unchecked, delete gated on `-not $Silent` AND the checkbox state, `-Silent` never building the checkbox), with the hardening pins on top (`$deleteUserData` assignment exclusivity, the confirm dialog's default-keep form of AcceptButton/initial-focus/tab-order, the install-dir fingerprint guard before any deletion, the remnant-gated registry-key removal reusing the keep-list — since #244 the same `$appRemnants` also feeds the closing dialog, whose remnants branch comes first, names the folder, says the Apps-list entry was kept on purpose, advises the hand-fix rather than a re-run the fingerprint guard could refuse, and shields kept data from a whole-folder delete — the non-recursive final dir removal, and `setup.ps1` accepting a keep-data-uninstall folder on reinstall); and the #181/#191 sandbox-harness contract — clipboard sharing off in the `.wsb`, the compiler-free injection route, the Notepad start being presence-probed *and* wrapped, the reported `injection-route=` token, the settings-window lane with its `Settings not opened` retry and the hard-failure flag that ends it, the checklist file and the launcher's pointer to it, the two-staged boot probe (a static ban on `vmwp`/`vmmem`, which WSL2 keeps alive on the maintainer's box, plus the exit-6 ordering), the `finally`-scoped cleanup filtered against a pre-launch id snapshot, a ban on `*.py:NNN` line citations, and two coupling-guard functions asserting three couplings: every polled log string still exists in `thoughtborne.py`, the registration line still matches `hotkey_manager.py`'s f-string, and the `[SETTINGS] visible:` needle is still a substring of `settings_visibility.format_visible_line`'s output. | `--show` (parsed denylist + shortcuts) |
| `test_text_cleanup.py` | The transcript cleanup functions (#242) — `_remove_spoken_fillers` (#31/#97: delimiter handling, the sentence-start capital move, the #101 quote exemption, the `Lähmung`/`ähnlich` word-boundary guards, and the cases where it deliberately does NOT put punctuation back), the Groq-only `_clean_groq_hallucinations` (phrase strip, length gate, the `" und"` fragment left as-is), and the two #242 fixes: a sentence's terminal mark survives a filler at its boundary (`!`/`?` now travel with the filler where a `.` always did), and the phrase list is consulted once more after a trailing fragment was stripped. Named only here: the source guard that keeps the calls alive in all three `transcribe` methods, and the pin that the pattern lists stay data. | `--show` (every case as input → output) |
| `test_typed_cap.py` | The #7/D-003 typed-insert length cap helper — `cap_typed_text`'s character math, the notice construction, and its ASCII / no-newline / ≤-cap guarantees. | — |
| `test_typed_cap_wiring.py` | That the same cap is actually WIRED into both typed routes (#242, D-003) — a recording keyboard stub drives the real `OutputManager` on the output thread's typed route and on the clipboard route's fallback to typing, asserting every `keyboard.write` payload is capped, notice-suffixed and honestly reported, while an everyday transcript and one of exactly cap length arrive byte-identical and untruncated. Replacing both call sites with the raw text passes every other driver in the ladder — that is the gap this one closes. | — |

## While the tool is running

If `thoughtborne.py` is currently running **from this checkout**, do not modify code, rename files, or otherwise disturb the working directory. An instance running from a *different* checkout of this repo does not block edits here — though it does own the global hotkeys, so don't launch the tool from this checkout while one runs elsewhere. Reliable check — the log heartbeat: the recording loop writes a `Recording loop alive` DEBUG line to that checkout's `thoughtborne.log` every 60 seconds, so the tool is running exactly when the log's mtime is fresh (under ~3 minutes) **and** its final lines do not say `Program ended`. Neither half alone suffices: a clean shutdown writes `Program ended` and still looks recently touched, and a stale mtime without that line just means the process died uncleanly. A process-list check (a Windows python process whose command line contains `thoughtborne.py`) is *not* reliable: when the tool runs elevated, a non-elevated query — e.g. `Get-CimInstance Win32_Process`, typical from WSL — sees the python process but an empty `CommandLine`, and the check falsely reports the tool as not running. Ask the user to stop it with `Ctrl+Alt+4` first. The user may be dictating into the same agent session that is being asked to edit — the hotkey exit is the clean handoff.

## Conventions

- **Language:** English for code, inline comments, commit messages, and all public documentation.
- **Bilingual README:** `README.md` (English) and `README.de.md` (German) are content-equivalent twins — a change to one is mirrored in the other.
- **Install-step sync:** the bilingual rule does not cover the install commands fully — they also live in `docs/index.html` and `docs/en/index.html` (hero and setup box each) and are described in prose in `llms-install.md`. A change to the install steps is mirrored across all of these in the same change (#60).
- **Commit messages:** short, imperative English; reference issues with `(#N)`.
- **CHANGELOG.md:** non-trivial changes get an entry under `## [Unreleased]` (Keep-a-Changelog categories: `### Added` / `### Changed` / `### Fixed` / `### Removed`).
- **Decision log:** `DECISIONS.md` records deliberate, contestable product decisions. Check it **before discussing or specifying any behavior change** — issue texts included — so a settled call is not silently reopened. An issue that touches a recorded decision cites it (`respects D-001` / `proposes superseding D-001`); superseding an entry needs the maintainer's okay (mark the old one `Superseded by D-NNN`, never delete it).

## Guardrails

- **Never commit `.env`**, and never reproduce API keys in committed files, logs, or chat output.
- **Hallucination-filter patterns** in `transcriber.py` (`_clean_groq_hallucinations`) are **data**, not prose. Never translate, paraphrase, or "improve" them.
- **Umlaut hotkey support is live machinery, not dead code.** No shipped default uses `ü` since the self-test moved to `Ctrl+Alt+T` (#211, D-012), but users still bind it via the #55 override surface — keep the `VkKeyScanW` path in `hotkey_manager.py`, the `KEY_SPECIAL` lane in `hotkey_parse.py`, the `udiaeresis` keysym in `settings_io.py`, and the `Ü` rendering in `thoughtborne_settings._pretty_one` / `console_ui`. Conversely, do not move a shipped default back onto a layout-resolved key.
- **Folders starting with `_`** (e.g. `_research/`, `_backups/`) are gitignored, local-only workspaces of whoever owns the checkout. Never auto-delete, never "clean up", never remove duplicates. Deletion only on explicit instruction.
- **Be conservative with working code.** Do not refactor or rewrite without a stated reason — "clean code is a feature" (`VISION.md`) justifies keeping code legible *while changing it for a reason*, not standalone rewrites. Direction and ambition live in `VISION.md`; this rule caps code churn, it is not a feature freeze.

## Where things live

- **Source:** `thoughtborne.py`, `audio_handler.py`, `transcriber.py`, `output_handler.py`, `hotkey_manager.py`, `hotkey_parse.py` (the shared, ctypes-free hotkey lexical layer — parsing, the static VK map, F-key support, #55 — imported by both `hotkey_manager` and `config`), `ptt_detector.py` (the push-to-talk gesture state machine, #66 — Win32-decoupled), `config.py`, `console_ui.py` (the Cockpit console renderer, #109 — pure/stdlib, verified by `test_console_ui.py`; its lineup is key-aware since #200 — a row whose key env var is absent renders dim, and it carries the keyless shop-window's yellow guidance line and the `render_keyless_notice` panel), `typed_cap.py` (the pure, stdlib-only typed-insert length cap — `cap_typed_text` plus the 4,000-char constant and its appended notice, #7 — imported by `output_handler`; off-Windows-testable, verified by `test_typed_cap.py`), `engine_memory.py` (the pure, stdlib-only memory of the last selected engine — the `runtime_state.json` read/write plus the startup precedence rule and the carousel rotation that follows from it, #193, D-008 — imported by `thoughtborne` and by the settings app, which shows the remembered engine and records the engine the #178 wizard preselect establishes; off-Windows-testable, verified by `test_engine_memory.py`), `restart_signal.py` (the pure, stdlib-only restart handshake between the settings app and the running tool — the `restart_request` signal file's write/consume (a sibling of `runtime_state.json`; existence is the signal, consume is an atomic delete so only a real removal shuts the tool down), the read-only single-instance-mutex liveness probe (`OpenMutexW`, never create/hold), and the tool's D-004 mutex name as the single source both sides share, #202 — imported by `thoughtborne` and the settings app; ctypes is lazy in the one probe so it loads off-Windows (D-005); off-Windows-testable, verified by `test_restart_signal.py`).
- **Settings app (#144):** `thoughtborne_settings.py` (the tkinter settings/onboarding window — pure stdlib, hands-on-verified per the test issue), `settings_io.py` / `key_check.py` (its file-IO+validation core and live key-check — pure/stdlib, no tkinter, verified by `test_settings_io.py`), `settings_strings.py` (the DE/EN UI string table plus `t()` — the app defaults to English with no system-language detection, D-015; its `engine.desc.*` EN descriptors track `config.API_DISPLAY`, so change both together, same rule as the README/console wording), `settings_instance.py` (the stdlib-only single-instance guard + focus-existing remedy — a distinct named mutex and an `EnumWindows` exact-title-match focus, ctypes imported lazily so it loads off-Windows, #196, D-009; since #203 the focus path returns a `FOCUS_*` outcome category the caller logs and raises the window with a cross-process topmost pulse that needs no foreground rights — verified by `test_settings_instance.py`), `settings_visibility.py` (the stdlib-only, tkinter-free helpers the #203 visibility fix extracted so the core ladder can check them off-Windows — `scrollbar_should_show` (the #180 auto-hide decision), `format_visible_line` (the `[SETTINGS] visible:` first-paint log line), and `wrap_length` (the shared wraplength formula) — plus, since #240, the whole `[SETTINGS]` log lane: `format_settings_line`, `format_error_block` and the guarded `append_log_line` every settings-side log write goes through, the crash blocks included; that sink is the one helper here that does IO, and it takes the log path as a parameter rather than importing `config`, which is what lets the ladder point it at a tempdir; `thoughtborne_settings` imports them, while the Tk-near parts stay in the app — the scroll-canvas idempotency guards and the #203 wrap mechanism, where every wrapping label's `<Configure>` defers to one coalesced `_push_wraps` so wraplength is set once per settled width, not re-measured at every width step — verified by `test_settings_visibility.py`), `settings_theme.py` (the #155 visual design: the `clam`-based ttk theme + design tokens — palette, fonts, the DPI `sp()` helper and the measured content-column width — that `apply_theme(root)` pins as the first step of the app's `__init__`; pure stdlib with tkinter imported lazily, so its constants and the WCAG contrast test load without a display; leaving the native `vista` theme is D-010, and it is the single place the two surfaces are defined — `TFrame` is the page surface *and* the #180 scroll canvas's background source, so change them together — verified by `test_settings_theme.py`).
- **Windows launcher:** `Thoughtborne.bat`. (The standalone settings launcher was retired in #223/D-014 — the settings window opens only from the running tool via `Ctrl+Alt+G` / the `--first-run` spawn.)
- **Installer (#76) & uninstaller (#209):** `setup.ps1` (the ASCII-only, BOM-free, iex-safe Windows installer shipped as a GitHub release asset — one-liner `irm … | iex` and double-click-ZIP lanes, uv-based, idempotent, verified by `test_setup.py`; since #209 it also writes the per-user Add/Remove-Programs registry entry — HKCU, registry cmdlets only, so no secrets and no config file, D-002/D-011 — on every install and in-place update, with `DisplayVersion` read from the installed `pyproject.toml`; its reinstall guard accepts a folder left holding only keep-data residue after a keep-data uninstall, so reinstalling into the same dir is not refused), `setup.bat` (the double-click/ZIP wrapper and paste-free in-place updater; sets `THOUGHTBORNE_FROM_BAT` so the `-File` lane returns a real exit code and can pause on failure), `uninstall.ps1` (the #209 GUI uninstaller — PowerShell + WinForms, no Python/venv, no admin, ASCII/no-BOM; hides its own console, refuses a running tool via the log heartbeat and never kills it, self-copies to `%TEMP%` and relaunches with the captured `-InstallDir`, checks that dir carries the Thoughtborne fingerprint before any deletion (a stray folder is never emptied) and names it in the dialog, then removes the two shortcuts → the install files → its own registry key last (kept when app files survive, so the leftover stays visible under Installed apps — a clean re-run is not promised, D-011; the final empty-dir removal is non-recursive so a misread can never take kept data); a sentinel-fenced keep-list keeps user data by default, deletion is an opt-in checkbox and the `-Silent` lane can never delete (D-011), and uv + its managed Python are left untouched; drift-guarded by `test_setup.py`), and `sandbox/` (the pre-release Windows-Sandbox verification harness — the host launcher `run-sandbox.ps1` starts the throwaway sandbox by CLI, waits for proof it came up (exit 6 when nothing ever runs; a sandbox that is alive but slow is waited out, never stopped mid-run), polls its verdict, then stops the sandbox it started and records the run in `HOST.txt`; `thoughtborne-install-test.wsb` is the portable template, `verify-in-sandbox.ps1` the in-sandbox driver — since #191 its chords are injected through an in-memory binding *designed* to need nothing present in the image (no C# compiler, no Notepad; verified as far as a host-side bind on PowerShell 5.1, never yet run inside a sandbox — see the harness README's *What it does not cover*), the detail line names the route it used, and after the self-test it opens the settings window and photographs it for a host-side visual check against `settings-shot-checklist.md` — reported, never gating; finished and drift-guarded by `test_setup.py` (#181/#191); `temp.env`, `out-*/`, and `*.local.wsb` are gitignored).
- **Test ladder:** the `test_*.py` drivers in the repo root (standalone, stdlib, off-Windows — each described under *Run & verify* above) and `run_tests.py`, the single runner that executes all of them as subprocesses and prints one verdict line each; it surfaces a driver's `(skipped ...)` notes, so a green run does not read as if the display-gated checks had passed when they were skipped. Since #242 every driver also carries a `def test_all()`, so a plain `python -m pytest` runs the ladder as one test per driver — next to the individual cases some drivers have always exposed as `test_*` functions, which then run twice; `run_tests.py` and CI stay the gate. `.github/workflows/tests.yml` runs that runner on every push to `main` and every pull request (Python 3.10 + 3.12, `xvfb-run` for the display-gated checks; `soundfile`, `groq` and `python-dotenv` are the ladder's only third-party dependencies off Windows — `groq` because it is `transcriber.py`'s one module-level third-party import, so the #206 Soniox constructor lane runs there for real instead of skipping, `python-dotenv` because it is the loader `config.py` reads `.env` with, so the #238 `.env` lanes run there for real too; `uv sync` cannot be used, `pyproject.toml` resolves for `sys_platform == 'win32'` only).
- **Public docs:** `README.md`, `README.de.md`, `CHANGELOG.md`, `DECISIONS.md`, `VISION.md`, `RELEASING.md` (the maintainer release ritual — tag, build the `git archive` install ZIP + `setup.ps1` assets, publish; #145, D-006), `build-release-zip.sh` (builds both release assets into gitignored `dist/` and dry-run-verifies the ZIP; never tags or publishes — #145), `LICENSE`, `AGENTS.md`, `llms-install.md`, `.env.example`, `personal_settings.example.json`.
- **Website:** `docs/` — the public project site at [thoughtborne.app](https://thoughtborne.app), deployed by `.github/workflows/deploy-site.yml` (an FTPS mirror of `docs/` to the production host on every `main` push that touches `docs/**`; #146). Public content, not a local workspace. Site code stays comment-lean: a comment must earn its place by recording an invariant or trap the code cannot show — sync couplings (the console masthead ported from `console_ui.py`, the inline SVG marks vs `assets/logo/`, the twin install one-liners), the `?v=` cache-buster discipline, the legal pages' noindex-but-crawlable mechanics, `line-height: 1` on half-block art, root-absolute hrefs not resolving under `file://`. No section markers, no design history, no rationale prose — that context belongs in commit messages and issues, and section structure is what class names are for.
- **User data** (created at runtime, gitignored): `history/` (recordings in `audio/`, transcripts in `transcripts/`), `thoughtborne.log`, and `runtime_state.json` (the tool's own record of the last selected engine, #193 — machine-written, but it carries a user choice) — never delete. (The state file says of itself that it is safe to delete, and that is true for its *owner*: deleting it costs only the remembered engine. It is still not yours to clean up.) Older checkouts may still carry the legacy `voice_archive/` + `text_archive/` folders (auto-migrated into `history/` at startup) — never delete those either.
