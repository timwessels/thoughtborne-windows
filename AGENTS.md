# AGENTS.md

Guidance for AI coding agents working in this repository. Plain Markdown — humans are welcome to read it too.

## What this is

Thoughtborne is a hotkey-driven voice-to-text tool for Windows, written in Python. Recording is started with `Ctrl+Alt+W`; one of `A` / `D` / `H` / `Y` (held with `Ctrl+Alt`) ends it and inserts the transcript at the cursor position in whatever Windows app is active. Four transcription APIs are switchable at runtime; the default is `soniox-live`. Windows-only by design (Win32 hotkeys, audio capture, text insertion).

- **Setting the tool up for a user?** Follow [`llms-install.md`](llms-install.md) — the guided setup, including API-key onboarding and the WSL2 pitfalls.
- **Vision & direction:** [`VISION.md`](VISION.md) — why the tool exists, the quality bar (*good enough to send to LLMs unread*), who it's for, decision principles, and non-goals. Calibrate judgment calls — scope, trade-offs, priorities — against it. Stability is principle #1; the quality floor and the no-training-on-user-data rule are hard gates. The target user, in one sentence: a self-reliant, German-dictating techie who wants no subscription, no lock-in, verifiable privacy, and a tool that bends to their workflow (full persona under "Who it's for" in `VISION.md`).
- **Mac port:** [`thoughtborne-macos`](https://github.com/timwessels/thoughtborne-macos) — three APIs instead of four, otherwise analogous. Useful as a reference for porting decisions.

## Run & verify

- Run: `uv run thoughtborne.py`, or double-click `Thoughtborne.bat`. Requires Windows, a microphone, and at least one API key in `.env` (template: `.env.example`).
- There is no automated test suite. The verification ladder: `python -m py_compile <changed files>` for syntax; `python3 test_console_ui.py` verifies every console panel/strip (widths, CP437 charset, the plain-ASCII twin, red-exclusivity — including the #166 ALREADY-RUNNING notice and the partial-hotkeys advisory, and the #200 key-aware lineup where a row whose key env var is absent renders dim (both the framed and compact lineup renderers, checked positionally), the keyless shop-window masthead's yellow guidance line, the calm keyless SETUP-NEEDED panel a hotkey press raises, the `(default)`-marker-gone guard across masthead/switched/switch-failed, and the pure `config.engine_has_key` predicate plus its `API_KEY_ENV == AVAILABLE_APIS` coverage guard) and runs on plain Python off-Windows (`--show` prints the screens, the shop-window masthead + keyless panel included); `python3 test_typed_cap.py` exercises the #7 typed-insert length cap off-Windows — the pure `cap_typed_text` character math, the notice construction and its ASCII/no-newline/≤-cap guarantees, cites D-003; `python3 test_audio_stall.py` exercises the audio stall/deadlock guards (#128 — the non-blocking capture read, the stall watchdog, and the close/open timeout-and-poison containment) off-Windows against a fault-injecting fake stream; `python3 test_retry_marker_lifecycle.py` exercises the persistent retry-marker lifecycle off-Windows (#106/#114/#134 announce-once + #133/#141 no-speech verdict + #138 generalizing that verdict to every engine on a single-engine basis, with the per-call error signal and its coarse reason category, cites D-001) against a tempdir archive; `python3 test_hotkey_overrides.py` exercises the #55 `personal_settings.json` hotkey/engine override surface off-Windows (the shared `hotkey_parse` lexical layer and `config.apply_hotkey_overrides`: partial override, warn-and-fallback on bad combos, duplicate/collision handling, F-key names, list values, and the per-box display-lead prefix, and the #211/D-012 static-default guard (every key in `DEFAULT_HOTKEYS` classifies `KEY_STATIC`, so no shipped default can regress onto a layout-resolved key)); `python3 test_settings_io.py` exercises the #144 settings-app IO core off-Windows (the surgical `.env` / `personal_settings.json` merge with its data-safety regressions, the pure hotkey-combo helpers, `key_check` classification, and now the `settings_strings` DE/EN string-table completeness (plus the D-015 guard that the retired `detect_ui_language()` stays gone) and the two on-demand merges — `ui.language`, and the #193/#198, D-008/D-002 three-valued `defaults.api` contract — an untouched engine field (`default_api=None`) leaves the file's pin byte-identical, invalid hand-typed values included, a fixed-mode pick writes the engine verbatim (the built-in default included), and remember-mode over a pin drops it via the distinct `REMOVE_API_PIN` signal — plus the pure `resolve_engine_save_signal` decision table that derives those two on-save signals off-Windows, and the #201 key-aware `engine_keyed` per-engine predicate (stored vs live field per provider, blank-over-stored stays keyed, all-keyless, unknown id — delegating to `config.engine_has_key` so the console and settings never disagree) with its named D-002 regression that an untouched fixed pin on a now-keyless engine still resolves to `(None, None)`, and the pure `resolve_save_action` decision table (first_run × has_key → `save`/`save_close`/`save_restart`, no `tool_running` axis and no `save_start` since #223/D-014 — the settings window opens only from a running tool, so a keyed save is always a restart — the token space asserted exactly, a keyed save → `save_restart` in both wizard and everyday mode, plus a guard that the retired `btn.save_start`/`footer.*` strings stay gone), and the #211 preset-parity guard (`PRESET_FKEYS`' four housekeeping actions — open_history / open_settings / test_transcription / exit_program — equal `config.DEFAULT_HOTKEYS`', the invariant its own comment promises and nothing enforced before), and the #233 push-to-talk toggle contract (D-002 addendum) — the three-valued `ptt_enabled` merge where an untouched toggle leaves the whole `push_to_talk` block byte-identical (a hand-typed invalid `enabled` included) and a bool writes ONLY `enabled`, so a hand-tuned block's `_comment`, trigger, insert path and three timings survive an off-and-on round trip and a fresh block carries the example's `_comment` lead and nothing beyond `enabled`, plus `read_ptt_enabled` (what the toggle displays: `config.py`'s JSON-boolean-only rule, so it can never show ON for a file the tool reads as OFF — with the shipped-default guard read from `config.py` as SOURCE, since the imported `config.PTT_ENABLED` is the effective value this checkout's own `personal_settings.json` overrides at import) and the pure `resolve_ptt_save_signal` table that byte-identity rests on, plus one save carrying all three on-demand keys at once and — read off `thoughtborne_settings.py`'s syntax tree, since the GUI is hands-on only — the two write call sites the guarantee actually hangs on: `_save` must pass the *resolved* signal rather than the raw toggle state, and `_persist_language`'s language-only write must pass no `ptt_enabled` at all; and the #205 key-check honesty contract (an *answered* status is never `UNREACHABLE` — swept across 200–599, so the rule is pinned rather than examples; `_check_bearer` really puts its explicit `User-Agent` on the wire, captured off a localhost socket, since urllib's default one is refused outright by Groq and was the whole cause of the false verdict; plus the static coupling guard that every `KeyStatus` member has a row in `_render_indicator`'s verdict table and a `test.<value>` string in both languages — a missing row is a `KeyError` inside the render, i.e. a silently dead Test-key button); `--show` prints a sample write); `python3 test_setup.py` verifies the #76 install-mechanics scripts (`setup.ps1` / `setup.bat`) off-Windows — statically, no PowerShell execution: ASCII/no-BOM, no bare `exit` (iex-safety), the user-data denylist, the fingerprint and running-instance guards, `-DryRun` gating, the Start-menu shortcut, no secret collection (D-002), the Astral-uv launcher fallbacks, and the #209 uninstall story — the `setup.ps1` per-user Apps-list registration (cmdlet-based, DryRun-gated, `DisplayVersion` parsed from the installed `pyproject.toml`, a real `DisplayIcon` whose asset exists, `QuietUninstallString` carrying `-Silent`) plus `uninstall.ps1` (ASCII/no-BOM, the keep-list covering user data while excluding `.venv` and `.env.example`, the console-hide, the never-kill running guard referencing `Ctrl+Alt+4`, the self-copy + captured-`-InstallDir` threading, the registry-key self-delete, shortcut removal, uv left untouched, and the D-011 data-safety contract: checkbox default unchecked, delete gated on `-not $Silent` AND the checkbox state, `-Silent` never building the checkbox), plus the #209 hardening pins — the `$deleteUserData` assignment exclusivity, the confirm-dialog default-keep form (AcceptButton/initial-focus/tab-order), the install-dir fingerprint guard before any deletion, the remnant-gated registry-key removal (reusing the keep-list so kept data never holds the entry alive), the non-recursive final dir removal, and `setup.ps1`'s reinstall-residue acceptance (a keep-data-uninstall folder is re-installable, not refused), plus the #191 sandbox-harness contract — clipboard sharing off in the `.wsb`, the compiler-free injection route, the Notepad start being presence-probed *and* wrapped (the unguarded one is what made both #181 E2E runs land PARTIAL), the reported `injection-route=` token, the settings-window lane with its `Settings not opened` retry and the hard-failure flag that ends that retry when the tool reports it could not open the app at all, the checklist file and the launcher's pointer to it, the two-staged boot probe (a static ban on `vmwp`/`vmmem`, which WSL2 keeps alive on the maintainer's box, plus the exit-6 ordering that keeps the boot window fail-fast for a dead sandbox and a grace wait for a live slow one), the `finally`-scoped cleanup filtered against a pre-launch id snapshot (derived stop → loop variable → assignment, so the filter cannot be deleted from the stop itself), a ban on `*.py:NNN` line citations anywhere in the harness, and the two coupling guards that keep the harness honest — every log string the driver polls for must still exist in `thoughtborne.py`, the registration line it parses must still match `hotkey_manager.py`'s f-string (rendered from that template, not a hand-written sample), and its `[SETTINGS] visible:` needle must still be a substring of `settings_visibility.format_visible_line`'s output (the real install/uninstall behavior is the hands-on Windows-Sandbox check under `sandbox/`); `python3 test_deps_sync.py` checks the dependency-declaration lanes stay in lockstep off-Windows (#173) — `pyproject.toml`'s `[project] dependencies` against `requirements.txt`, compared as a normalized requirement set (case-insensitive name, whitespace-insensitive specifier) and naming the drifting package and which file holds which version on a mismatch; pure stdlib, Python 3.10+ (`tomllib` where present, a small line-parser fallback on 3.10, the two cross-checked when both are available); `python3 test_engine_memory.py` exercises the #193 memory of the last selected engine off-Windows (the `runtime_state.json` round-trip and its atomic, leftover-free write, the `AVAILABLE_APIS` whitelist applied on both the read and the write side, the corrupt/stale/unwritable-directory fallbacks that must never cost a start, the pure startup precedence rule — an explicit `defaults.api` outranks the memory, cites D-008 — the shipped carousel rotation `thoughtborne.py` starts from, and a static guard on both directions of `config.py`'s `DEFAULT_API_IS_EXPLICIT` — `= True` at module level, since as a function local it would invert D-008 in silence, and exactly one top-level `= False` initializer ahead of it, since initializing it True would make every install look configured and kill the memory) against a tempdir state file; `python3 test_restart_signal.py` exercises the #202 settings-app→tool restart handshake off-Windows (the `restart_request` signal-path contract as a sibling of `runtime_state.json`, the write/consume round-trip, and the two invariants that must never regress — NO shutdown without a successful consume (a vanished file → `FileNotFoundError` → False, an undeletable file → `PermissionError` → False, so an unremovable signal can never loop a restart) and the probe never creating/holding the mutex (fail-open to False off-Windows) — plus the constants (`TOOL_MUTEX_NAME == "Thoughtborne-SingleInstance"`, distinct from `settings_instance.SETTINGS_MUTEX_NAME`, `wait > interval`) and static source guards on `thoughtborne.py` that the mutex-name hoist is used, the old literal is gone, and `consume_restart_signal` is wired in both the startup guard and the recording loop; the live mutex probe + end-to-end restart are hands-on) against a tempdir; `python3 test_settings_instance.py` exercises the #196/D-009 settings single-instance guard off-Windows (that `import settings_instance` succeeds with its ctypes lazy, that `settings_window_titles()` returns exactly the four localized window titles computed from `settings_strings`, that `SETTINGS_MUTEX_NAME` stays distinct from the tool's `Thoughtborne-SingleInstance` and session-scoped, that the four `FOCUS_*` outcome categories are distinct non-empty strings (#203, logged verbatim), and that the Windows mutex/focus functions fail open off-Windows — `create_instance_mutex()` → `(None, False)`, `focus_existing_settings_window()` → `FOCUS_NOT_FOUND` (a category string since #203, no longer a bool); the real ctypes mutex/focus is hands-on, #199); `python3 test_settings_visibility.py` exercises the #203 tkinter-free settings-visibility helpers off-Windows (`scrollbar_should_show(lo, hi)` — the #180 scroll-canvas auto-hide decision, the single source of the overflow rule the idempotency guard flips on — across the fits/overflow/scrolled boundary cases, `format_visible_line(...)` — the `[SETTINGS] visible:` first-paint log line — full, fail-open when the spawn stamp is missing (timing fields drop), and partial when a best-effort probe reads `?`; and `wrap_length(width, margin, floor)` — the shared wraplength formula; `--show` prints sample lines) — and, WITH a display (skipped cleanly on the headless WSL box, like `test_settings_theme.py`), a regression check that builds the REAL app and asserts (a) the #180/#203 auto-hide idempotency (each scrollbar's `grid`/`grid_remove` sequence strictly alternates — the `_bar_shown` latch flips a bar only on a real overflow-state change; a churn *count* ceiling no longer works since the wrap deferral masks the storm the missing latch used to cause) and (b) the #203 wrap-deferral invariant — some label is wrapped and every wraplength write goes through the coalesced `_push_wraps`, never a per-`<Configure>` set (the Windows stall). The GDI-remeasurement *cost* is Windows-only (the maintainer's map→expose measurement is the perf proof); the test guards the fix's shape, plus a dead-guard check that a genuine resize still re-wraps, and a #216 display-gated maximize→restore check that drives the 800→3400→800 width cycle on the Overview and Provider tabs and asserts the active tab body stays mapped, centred (physical x == recorded item x) and inside the viewport with an origin-anchored scrollregion (`x1==0`), failing on the unfixed code with the frozen-state signature — guarding against the scrollregion/`confine` freeze recurring (#216), and a #231 display-gated verdict-wrap check that delivers every key-test verdict to both provider cards and asserts the line is not clipped at the column edge (the bug), has not collapsed to the wraplength floor (the trap the obvious fix falls into — a label packed without `fill="x"` gets its own requested width from pack, so wrapping feeds back on itself), keeps a width independent of its text (the mechanism `fill="x"` provides), and really wraps whenever the text outgrows the label, with the #179 Soniox balance note covered alongside it and a dead guard that a genuine width change still re-wraps once per settled width — all thresholds relative, never pixel constants, and verified red on both the unfixed and the naively fixed packing; `python3 test_settings_theme.py` verifies the #155 settings-app theme module off-Windows (that `import settings_theme` succeeds with tkinter imported lazily, that every palette constant is a `#RRGGBB` string, that every text colour clears WCAG AA — ≥ 4.5:1 — on both the page and card surfaces and `CONTROL_LINE` clears the 3:1 non-text floor (WCAG 1.4.11 — the guard that catches a future "lighten the grey a bit"), that the type-ladder floor holds (`SIZE_BODY` ≥ 10 and small→title strictly increasing), and — with a display available — that `apply_theme` really pins `clam` and `Style().lookup("TFrame", "background")` equals `PAGE`, the #180 sync invariant since the scroll canvas paints from it; the display checks skip cleanly on a headless box, cites D-010); start the tool and check the startup masthead plus `All hotkeys registered successfully` in `thoughtborne.log`; the in-app self-test `Ctrl+Alt+T` transcribes `test_audio.mp3` end to end (needs a valid API key).
- Working from inside WSL2? The tool itself must run Windows-side; [`llms-install.md`](llms-install.md) covers the interop.

## While the tool is running

If `thoughtborne.py` is currently running **from this checkout**, do not modify code, rename files, or otherwise disturb the working directory. An instance running from a *different* checkout of this repo does not block edits here — though it does own the global hotkeys, so don't launch the tool from this checkout while one runs elsewhere. Reliable check — the log heartbeat: the recording loop writes a `Recording loop alive` DEBUG line to that checkout's `thoughtborne.log` every 60 seconds, so the tool is running exactly when the log's mtime is fresh (under ~3 minutes) **and** its final lines do not say `Program ended`. Neither half alone suffices: a clean shutdown writes `Program ended` and still looks recently touched, and a stale mtime without that line just means the process died uncleanly. A process-list check (a Windows python process whose command line contains `thoughtborne.py`) is *not* reliable: when the tool runs elevated, a non-elevated query — e.g. `Get-CimInstance Win32_Process`, typical from WSL — sees the python process but an empty `CommandLine`, and the check falsely reports the tool as not running. Ask the user to stop it with `Ctrl+Alt+4` first. The user may be dictating into the same agent session that is being asked to edit — the hotkey exit is the clean handoff.

## Conventions

- **Language:** English for code, inline comments, commit messages, and all public documentation.
- **Bilingual README:** `README.md` (English) and `README.de.md` (German) are content-equivalent twins — a change to one is mirrored in the other.
- **Commit messages:** short, imperative English; reference issues with `(#N)`.
- **CHANGELOG.md:** non-trivial changes get an entry under `## [Unreleased]` (Keep-a-Changelog categories: `### Added` / `### Changed` / `### Fixed` / `### Removed`).
- **Decision log:** `DECISIONS.md` records deliberate, contestable product decisions. Check it **before discussing or specifying any behavior change** — issue texts included — so a settled call is not silently reopened. An issue that touches a recorded decision cites it (`respects D-001` / `proposes superseding D-001`); superseding an entry needs the maintainer's okay (mark the old one `Superseded by D-NNN`, never delete it).

## Guardrails

- **Never commit `.env`**, and never reproduce API keys in committed files, logs, or chat output.
- **Hallucination-filter patterns** in `transcriber.py` (`_clean_groq_hallucinations`) are **data**, not prose. Never translate, paraphrase, or "improve" them.
- **Umlaut hotkey support is live machinery, not dead code.** No shipped default uses `ü` since the self-test moved to `Ctrl+Alt+T` (#211, D-012), but users still bind it via the #55 override surface — keep the `VkKeyScanW` path in `hotkey_manager.py`, the `KEY_SPECIAL` lane in `hotkey_parse.py`, the `udiaeresis` keysym in `settings_io.py`, and the `Ü` rendering in `thoughtborne_settings._pretty_one` / `console_ui`. Conversely, do not move a shipped default back onto a layout-resolved key.
- **Folders starting with `_`** (e.g. `_research/`, `_backups/`) are gitignored, local-only workspaces of whoever owns the checkout. Never auto-delete, never "clean up", never remove duplicates. Deletion only on explicit instruction.
- **Be conservative with working code.** Do not refactor or rewrite without a stated reason — "clean code is a feature" (`VISION.md`) justifies keeping code legible *while changing it for a reason*, not standalone rewrites. Direction and ambition live in `VISION.md`; this rule caps code churn, it is not a feature freeze.

## Where things live

- **Source:** `thoughtborne.py`, `audio_handler.py`, `transcriber.py`, `output_handler.py`, `hotkey_manager.py`, `hotkey_parse.py` (the shared, ctypes-free hotkey lexical layer — parsing, the static VK map, F-key support, #55 — imported by both `hotkey_manager` and `config`), `ptt_detector.py` (the push-to-talk gesture state machine, #66 — Win32-decoupled), `config.py`, `console_ui.py` (the Cockpit console renderer, #109 — pure/stdlib, verified by `test_console_ui.py`; its lineup is key-aware since #200 — a row whose key env var is absent renders dim, and it carries the keyless shop-window's yellow guidance line and the `render_keyless_notice` panel), `typed_cap.py` (the pure, stdlib-only typed-insert length cap — `cap_typed_text` plus the 4,000-char constant and its appended notice, #7 — imported by `output_handler`; off-Windows-testable, verified by `test_typed_cap.py`), `engine_memory.py` (the pure, stdlib-only memory of the last selected engine — the `runtime_state.json` read/write plus the startup precedence rule and the carousel rotation that follows from it, #193, D-008 — imported by `thoughtborne` and by the settings app, which shows the remembered engine and records the engine the #178 wizard preselect establishes; off-Windows-testable, verified by `test_engine_memory.py`), `restart_signal.py` (the pure, stdlib-only restart handshake between the settings app and the running tool — the `restart_request` signal file's write/consume (a sibling of `runtime_state.json`; existence is the signal, consume is an atomic delete so only a real removal shuts the tool down), the read-only single-instance-mutex liveness probe (`OpenMutexW`, never create/hold), and the tool's D-004 mutex name as the single source both sides share, #202 — imported by `thoughtborne` and the settings app; ctypes is lazy in the one probe so it loads off-Windows (D-005); off-Windows-testable, verified by `test_restart_signal.py`).
- **Settings app (#144):** `thoughtborne_settings.py` (the tkinter settings/onboarding window — pure stdlib, hands-on-verified per the test issue), `settings_io.py` / `key_check.py` (its file-IO+validation core and live key-check — pure/stdlib, no tkinter, verified by `test_settings_io.py`), `settings_strings.py` (the DE/EN UI string table plus `t()` — the app defaults to English with no system-language detection, D-015; its `engine.desc.*` EN descriptors track `config.API_DISPLAY`, so change both together, same rule as the README/console wording), `settings_instance.py` (the stdlib-only single-instance guard + focus-existing remedy — a distinct named mutex and an `EnumWindows` exact-title-match focus, ctypes imported lazily so it loads off-Windows, #196, D-009; since #203 the focus path returns a `FOCUS_*` outcome category the caller logs and raises the window with a cross-process topmost pulse that needs no foreground rights — verified by `test_settings_instance.py`), `settings_visibility.py` (the pure, stdlib-only, tkinter-free helpers the #203 visibility fix extracted so the core ladder can check them off-Windows — `scrollbar_should_show` (the #180 auto-hide decision), `format_visible_line` (the `[SETTINGS] visible:` first-paint log line), and `wrap_length` (the shared wraplength formula); `thoughtborne_settings` imports them, while the Tk-near parts stay in the app — the scroll-canvas idempotency guards and the #203 wrap mechanism, where every wrapping label's `<Configure>` defers to one coalesced `_push_wraps` so wraplength is set once per settled width, not re-measured at every width step — verified by `test_settings_visibility.py`), `settings_theme.py` (the #155 visual design: the `clam`-based ttk theme + design tokens — palette, fonts, the DPI `sp()` helper and the measured content-column width — that `apply_theme(root)` pins as the first step of the app's `__init__`; pure stdlib with tkinter imported lazily, so its constants and the WCAG contrast test load without a display; leaving the native `vista` theme is D-010, and it is the single place the two surfaces are defined — `TFrame` is the page surface *and* the #180 scroll canvas's background source, so change them together — verified by `test_settings_theme.py`).
- **Windows launcher:** `Thoughtborne.bat`. (The standalone settings launcher was retired in #223/D-014 — the settings window opens only from the running tool via `Ctrl+Alt+G` / the `--first-run` spawn.)
- **Installer (#76) & uninstaller (#209):** `setup.ps1` (the ASCII-only, BOM-free, iex-safe Windows installer shipped as a GitHub release asset — one-liner `irm … | iex` and double-click-ZIP lanes, uv-based, idempotent, verified by `test_setup.py`; since #209 it also writes the per-user Add/Remove-Programs registry entry — HKCU, registry cmdlets only, so no secrets and no config file, D-002/D-011 — on every install and in-place update, with `DisplayVersion` read from the installed `pyproject.toml`; its reinstall guard accepts a folder left holding only keep-data residue after a keep-data uninstall, so reinstalling into the same dir is not refused), `setup.bat` (the double-click/ZIP wrapper and paste-free in-place updater; sets `THOUGHTBORNE_FROM_BAT` so the `-File` lane returns a real exit code and can pause on failure), `uninstall.ps1` (the #209 GUI uninstaller — PowerShell + WinForms, no Python/venv, no admin, ASCII/no-BOM; hides its own console, refuses a running tool via the log heartbeat and never kills it, self-copies to `%TEMP%` and relaunches with the captured `-InstallDir`, checks that dir carries the Thoughtborne fingerprint before any deletion (a stray folder is never emptied) and names it in the dialog, then removes the two shortcuts → the install files → its own registry key last (kept when app files survive, so Uninstall can be re-run; the final empty-dir removal is non-recursive so a misread can never take kept data); a sentinel-fenced keep-list keeps user data by default, deletion is an opt-in checkbox and the `-Silent` lane can never delete (D-011), and uv + its managed Python are left untouched; drift-guarded by `test_setup.py`), and `sandbox/` (the pre-release Windows-Sandbox verification harness — the host launcher `run-sandbox.ps1` starts the throwaway sandbox by CLI, waits for proof it came up (exit 6 when nothing ever runs; a sandbox that is alive but slow is waited out, never stopped mid-run), polls its verdict, then stops the sandbox it started and records the run in `HOST.txt`; `thoughtborne-install-test.wsb` is the portable template, `verify-in-sandbox.ps1` the in-sandbox driver — since #191 its chords are injected through an in-memory binding *designed* to need nothing present in the image (no C# compiler, no Notepad; verified as far as a host-side bind on PowerShell 5.1, never yet run inside a sandbox — see the harness README's *What it does not cover*), the detail line names the route it used, and after the self-test it opens the settings window and photographs it for a host-side visual check against `settings-shot-checklist.md` — reported, never gating; finished and drift-guarded by `test_setup.py` (#181/#191); `temp.env`, `out-*/`, and `*.local.wsb` are gitignored).
- **Public docs:** `README.md`, `README.de.md`, `CHANGELOG.md`, `DECISIONS.md`, `VISION.md`, `RELEASING.md` (the maintainer release ritual — tag, build the `git archive` install ZIP + `setup.ps1` assets, publish; #145, D-006), `build-release-zip.sh` (builds both release assets into gitignored `dist/` and dry-run-verifies the ZIP; never tags or publishes — #145), `LICENSE`, `AGENTS.md`, `llms-install.md`, `.env.example`, `personal_settings.example.json`.
- **Website:** `docs/` — the public project site at [thoughtborne.app](https://thoughtborne.app), deployed by `.github/workflows/deploy-site.yml` (an FTPS mirror of `docs/` to the production host on every `main` push that touches `docs/**`; #146). Public content, not a local workspace.
- **User data** (created at runtime, gitignored): `history/` (recordings in `audio/`, transcripts in `transcripts/`), `thoughtborne.log`, and `runtime_state.json` (the tool's own record of the last selected engine, #193 — machine-written, but it carries a user choice) — never delete. (The state file says of itself that it is safe to delete, and that is true for its *owner*: deleting it costs only the remembered engine. It is still not yours to clean up.) Older checkouts may still carry the legacy `voice_archive/` + `text_archive/` folders (auto-migrated into `history/` at startup) — never delete those either.
